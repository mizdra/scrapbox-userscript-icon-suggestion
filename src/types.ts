export type FormData = {
  query: string;
};

export type Icon = {
  pagePath: string;
  imgAlt: string;
  imgTitle: string;
  imgSrc: string;
};

export type CursorPosition = {
  left: number;
  styleTop: number;
  styleLeft: number;
};
